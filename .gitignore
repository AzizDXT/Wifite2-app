# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.iml
.idea/

# Generated platform host folders (run `flutter create .` to regenerate)
/android/
/ios/
/linux/
/macos/
/windows/
/web/

# Release payload (OneShot source + arm64 binaries) — built by
# scripts/prepare_assets.sh and uploaded to a GitHub release, NOT committed
/dist/

# Vendored external sources / temp
external/
/tmp/

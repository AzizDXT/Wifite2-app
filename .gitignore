# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.iml
.idea/

# Generated platform host folders (run `flutter create .` to regenerate)
/android/
/ios/
/linux/
/macos/
/windows/
/web/

# Bundled payload (OneShot source + arm64 binaries) — built locally by
# scripts/prepare_assets.sh, NOT committed
assets/payload.zip

# Vendored external sources / temp
external/
/tmp/
